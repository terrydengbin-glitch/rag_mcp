# AGENTS.md

本项目是 CEK-TA 支持层项目，用于为其他交易、回测、模拟盘、实盘、LLM、RAG 项目提供专业知识库、Codex 专家工作流、MCP/RAG 检索能力、Vue3 知识审计界面、外部项目接入和知识倒灌机制。

## 每次任务启动规则

每次处理本项目中的用户请求前，必须先遵循本文件，并按需读取：

```text
docs/index_tasks.md
docs/tasks/README.md
```

如果请求涉及某个 Phase，必须读取对应任务卡。后续新增 Phase 任务卡后，按 `docs/tasks/README.md` 中的映射读取。

## 开发总原则

```text
1. 先任务，后开发。
2. 先上下游，后实现。
3. 先契约，后代码。
4. 先边界，后扩展。
5. 先 DoD，后标记 done。
6. 先测试，后交付。
7. 重大决策先向开发者提问确认。
```

## 任务管理规范

所有开发必须从任务管理入口开始：

```text
docs/index_tasks.md
docs/tasks/
```

开发前必须确认：

```text
1. 当前属于哪个 Phase。
2. 当前任务 ID 是什么。
3. 当前任务状态是什么。
4. 上游依赖是否完成。
5. 下游会消费什么交付物。
6. 是否已有对应任务卡。
```

没有任务卡的任务，不能直接开工。若任务不存在，先补充 `docs/index_tasks.md`、`docs/tasks/README.md` 和对应任务卡。

## 任务卡规范

每个 Phase 或重要任务必须有任务卡。任务卡必须包含：

```text
任务目标
上游输入
下游输出
输入契约
输出契约
边界范围
不做什么
涉及组件
涉及数据结构
涉及数据库/存储
实施步骤
Definition of Done
测试与验收
风险与回滚
需要开发者确认的问题
```

## 上下游对齐规范

开发前必须回答：

```text
这个任务依赖谁？
这个任务产物给谁用？
输入字段是什么？
输出字段是什么？
谁会读取这个文件/API/组件/schema？
改动会不会影响后续 Phase？
```

如果上下游不明确，先停止实现，补充任务卡或向开发者提问。

## 契约优先规范

任何实现前先定义契约，包括：

```text
文档结构契约
知识条目 schema
API 输入输出
MCP tool 参数和返回值
Vue3 组件 props/events
数据库表结构
文件目录规范
状态流
错误处理
审计状态
```

没有契约，不写实现。

## 边界规范

每个任务必须明确范围内和范围外。发现需求越界时，先更新任务卡，不直接扩大实现范围。

## 路径 Resolver 规范

本项目是支持层项目，会被其他项目通过本地路径、Git submodule、MCP、Skill 或 Plugin 复用。代码中不得把 `E:\collector\rag` 这类开发机绝对路径写成运行时依赖。

硬规则：

```text
1. Python 代码定位 CEK-TA 根目录时必须优先使用 `codex-expert-kit/core/path_resolver.py`。
2. 新增脚本、MCP tool、索引生成器、测试 fixture 路径时，必须通过 resolver 或显式环境变量解析。
3. 允许文档示例展示本机路径，但必须说明可通过 `CEK_TA_ROOT` 或项目相对路径替换。
4. 运行时默认路径不能依赖当前工作目录。
5. 如果需要访问仓库内文件，使用 `resolve_repo_path(...)`，不要手写 `parents[n]` 或硬编码盘符。
6. 外部项目接入时，优先配置 `CEK_TA_ROOT`，必要时配置 `CEK_TA_KNOWLEDGE_ITEMS_PATH`。
```

禁止事项：

```text
禁止在运行时代码中硬编码开发机绝对路径。
禁止在多个模块重复实现根目录推导逻辑。
禁止假设 CEK-TA 一定在 E 盘或固定目录名下。
禁止让 MCP、索引生成、测试依赖调用命令时的当前工作目录。
```

## UTF-8 编码规范

本项目包含大量中文 Markdown 文档，所有读写必须使用 UTF-8，避免中文乱码。

硬规则：

```text
1. 读取 Markdown、JSON、YAML、TOML、文本配置时优先显式使用 UTF-8。
2. 写入或修改中文文档时必须保持 UTF-8 编码。
3. PowerShell 读取中文文件时使用 `Get-Content -Encoding UTF8`。
4. PowerShell 写入中文文件时必须确保输出为 UTF-8，不依赖 Windows 默认编码。
5. Python 读写中文文件时必须显式使用 `encoding="utf-8"`。
6. Node.js 读写中文文件时必须显式使用 `utf8`。
7. 发现乱码时，先判断编码问题，不要直接按乱码内容修改文档。
```

禁止事项：

```text
禁止使用未声明编码的命令批量重写中文文档。
禁止把已经正常的 UTF-8 中文内容保存成 ANSI/GBK。
禁止在看到乱码时直接编辑乱码文本。
```

## 知识库开发规范

任何专业知识入库必须满足：

```text
有来源
有 source_type
有 domain/subdomain
有适用范围
有不适用场景
有 confidence
有 freshness
有 review_status
有冲突检查结果
```

禁止无来源知识入库。禁止把业务项目私有字段写入通用知识。禁止存在未标注适用边界的矛盾规则。

## 候选知识审计优化工作流

处理候选知识、AI 审计结果、知识优化、候选转正式知识时，必须优先按 Phase 32 的候选到 reviewed 知识批量审计工作流执行。

固定链路：

```text
候选采集 -> 导出 AI 审计包 -> 外部 AI 审计 -> 导入审计结果 -> Codex 按补丁点优化正式知识 -> candidate 标记 accepted/workflow -> formal knowledge 标记 reviewed -> 重建索引和前端 fixture -> 人工另行决定是否 approved
```

硬规则：

```text
1. candidate 不等于正式知识。
2. accepted_for_draft 不等于 approved。
3. reviewed 不等于 approved，也不自动进入默认指导。
4. AI 审计结果必须回到人工/Codex 治理流程，不得直接写 approved。
5. 候选源文件不删除，必须保留审计追踪和 formal knowledge 回链。
6. Vue3 候选页默认显示待审计队列，已通过/已沉淀候选进入独立分组。
7. MCP/SearchLab/知识树读取正式知识索引，不从候选队列当默认知识读取。
```

后续类似“审计通过、优化知识、批量回写、候选移出待审计队列”的请求，默认检查：

```text
docs/tasks/phase32_candidate_to_reviewed_workflow.md
.agents/skills/cek-ta-development-workflow/SKILL.md
```

## 知识倒灌规范

其他项目反哺知识时，不能直接写入 CEK-TA。必须先生成倒灌任务卡，并经过：

```text
proposed -> sanitized -> sourced -> classified -> conflict_checked -> reviewed -> accepted
```

## Vue3 开发规范

Vue3 界面是知识审计工作台，不是展示页。组件开发前必须定义：

```text
组件职责
props
emits
数据来源
状态
loading/empty/error
权限或审计动作
测试方式
```

前端页面展示语言硬规则：

```text
1. 项目开发的前端页面必须以中文作为默认展示语言。
2. 只要是用户会看到的内容，都必须输出中文，包括导航、标题、按钮、表单、占位符、提示、空状态、错误信息、弹窗、抽屉、表格列名、图表标签、状态标签、审计说明和帮助文案。
3. 代码变量名、接口字段名、协议名、路径、命令、日志原始字段、英文专有名词和来源引用可以保留原文，但必须在用户需要理解时提供中文说明。
4. 不得把英文 demo 文案、占位文案、mock 文案直接暴露到用户界面。
5. 前端验收时必须检查用户可见文案是否中文化，发现英文裸露要作为 UI 文案缺陷处理。
```

## 数据库/存储规范

涉及数据库或存储前，必须先定义数据模型、主键、索引、状态字段、时间字段、来源字段、审计字段、版本字段、迁移方式和回滚方式。

不能先写代码再补 schema。

## MCP/API 开发规范

MCP/API 默认只暴露必要能力。知识检索默认只读，审计动作必须显式记录，返回值必须带 source/citation/confidence。

每个 tool 必须定义：

```text
name
purpose
input schema
output schema
error schema
permissions
rate limit
test cases
```

## Definition of Done

每个任务完成必须满足：

```text
1. 交付物存在。
2. 路径和索引已更新。
3. 上下游已说明。
4. 契约已写清楚。
5. 边界已写清楚。
6. 测试已执行或说明无法执行。
7. 文档链接可追踪。
8. 任务状态已更新。
9. 如有风险，已写回滚方案。
10. 涉及中文文档时已确认 UTF-8 编码无乱码。
```

没有 DoD，不允许标记 `done`。

## 测试规范

按任务类型选择测试：

```text
文档任务：文件存在性、关键章节、链接、索引一致性
中文文档任务：UTF-8 读取检查、中文显示检查、无乱码检查
Schema/API/MCP：schema 校验、示例输入输出、错误输入、边界参数
Vue3：build、lint、单元测试、页面渲染、空状态、错误状态、桌面/移动端布局
知识任务：来源完整性、domain 分类、冲突检测、review_status
```

## 重大决策提问规范

遇到以下情况必须向开发者提问确认：

```text
引入新的数据库
引入新的后端框架
改变 Phase 顺序
修改任务 ID 或状态规则
删除或废弃已批准规则
把项目私有经验提升为通用规则
接受存在理论冲突的知识
改变 Vue3 信息架构
改变 MCP tool 权限
新增外部服务依赖
做不可逆迁移
```

小范围文档补充、任务卡细化、明显缺失的索引更新，可以直接做。

## 状态更新规范

每次完成开发后必须更新相关索引：

```text
docs/index_tasks.md
docs/tasks/README.md
对应 Phase 任务卡
```

如果新增文档，还要更新：

```text
README.md
相关入口文档
```

## 输出规范

每次完成任务后必须说明：

```text
改了什么
交付物在哪里
测试了什么
有没有未完成项
下一步建议是什么
```

用户展示语言规则：

```text
给用户展示的内容必须使用中文书写。
项目开发的前端页面必须输出中文；只要是用户会看到的内容，都按中文文案处理。
代码、路径、命令、字段名、协议名、英文专有名词和引用原文可保留原文，但需要用户理解时必须配中文说明。
如果用户明确要求其他语言，才按用户要求切换。
```

## 禁止事项

```text
禁止没有任务卡直接开发。
禁止没有契约直接实现。
禁止没有 DoD 标记 done。
禁止无来源知识入库。
禁止项目私有字段污染通用知识。
禁止未审计冲突规则进入 approved。
禁止直接操作实盘或密钥。
禁止随意扩大任务范围。
禁止改动后不更新索引。
禁止遇到重大决策不问开发者。
禁止使用错误编码导致中文文档乱码。
```
